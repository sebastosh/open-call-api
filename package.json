{
  "name": "open-call",
  "version": "1.0.0",
  "description": "marketplace web app allowing artists to submit works for review.",
  "main": "index.js",
  "repository": "https://github.com/sebastosh/open-call",
  "author": "sebastosh@gmail.com",
  "license": "MIT",
  "scripts": {
    "build": "cd client && yarn install && yarn build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "yarn build && yarn deploy && echo 'Client built!'"
  }
}
